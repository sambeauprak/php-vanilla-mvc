<!DOCTYPE html>
<html class="h-full bg-white dark:bg-gray-900" lang="fr">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>{{ page.name }}</title>
		<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

	</head>

	<body class="h-full">
		{% for block in blocks %}
			{% include "blocks/components/" ~ block.slug ~ ".html.twig" %}
		{% endfor %}
	</body>
</html>
